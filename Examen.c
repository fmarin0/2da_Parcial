#include <stdio.h>

int pseudo(int list, int n)
	int resultado;
{
        for (i=1,i>n-1,i++){
                min = i;
		return min;
                for (j=i+1,j>n,i++)
                        if (list[j] < list[min]) {
                                min = j;

			}
	}
